Hard line- separation it means the classes are separated by very strict line which either makes majority of the instances either into positive class or the negative class. Sensitive to outliers.

Soft line classification when we separate the classes with a less precise border here the accuracy decrease on the training data but generalize better on new instances. 