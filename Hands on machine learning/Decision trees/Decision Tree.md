Decision tree is a very visually intuitive to understand what's goin on.

Decision trees take very much time in training for numerical values. Decision trees are very fast and can be used in large scale products

How to find the probability of the class 
$P_{i,k}=$
The main training goal is to decrease the cost function- gini/entropy 

Entropy gives a more balanced tree but computationally expensive on the other hand we have gini which is bit faster.
$entropy=\sum^{k=1}_{n}P_{i,k}\log(P_{i,k})$

![[Pasted image 20260124230341.png]]

### [[Decision Tree hyper parameter]]
