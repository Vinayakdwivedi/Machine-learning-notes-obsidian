1) Increase in computational cost
2) Sometimes makes the model inaccurate
3) Data cannot be visualized
4) Data becomes sparse and more scattered that is a problem for the ml model which depends on distance of the points KNN,SVM.
For that reasons we need to reduce the dimensions of the data so that we can visualize the data. To reduce dimension we can either do feature selection or feature/dimensionality reduction.
Feature selection is done after the model is trained as we check the accuracy of the model and then find the most relevant features.
Methods to do ==Dimensionality Reduction==
- [[Manifold learning]] 
- [[PCA]] 