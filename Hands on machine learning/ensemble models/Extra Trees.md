We make the trees in the random forest more random by, selecting the threshold randomly for each node of the decision tree. and we have a class called ExtraTreeClassifier this helps to grow the trees faster as major time is spent over selecting thresholds.

Weather random forest is better or extra tree, you can only check it by testing it. 