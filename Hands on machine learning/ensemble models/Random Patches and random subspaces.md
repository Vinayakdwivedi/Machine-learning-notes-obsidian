We can also sample features using max_features and bootstrap_features= true
sampling both instances and features is called as *random patches*
keeping instances same but sampling features is called as *random subspaces* method.
It is done when there are many dimension like a image dataset like MNIST.