**Estimators**- these are the sklearn classes which are used to estimate. it uses *fit()* method for training, *simple imputer* is an example of this 
**Transformers** - This is another type of estimators which transforms the data it also uses *fit()* *transform()*.
**Predictor** - Estimators which predict data by *fit()*, linear regression